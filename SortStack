Given a stack of integers st[]. Sort the stack in ascending order (smallest element at the bottom and largest at the top).----sort in same stack st
https://www.geeksforgeeks.org/problems/sort-a-stack/1

class Solution:
    def sortStack(self, st):
        # code here 
        temp=[]
        
        while st:
            temp.append(st.pop())
            
        #Base case
        st.append(temp.pop())
    
        #Repetition 1
        while temp:
            val=temp.pop()
            while st and st[-1] > val :
                temp.append(st.pop())
            st.append(val)
                
        return st
